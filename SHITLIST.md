# Shitlist

List of things in need of fixing.

- package.lock, despite intent to use bun. need to ensure the frontend is fully bun-ified
- seems like there's still some sveltekit laying around?
- outside of dev mode, this should be fully hosted by the fastapi backend rather than a separate vite server
- venv/ still present. seems like some pip stuff from before the switch to uv
